<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(24,113,191,0.4)), url(<%= image_path('background.jpg') %>);" width: vh>
  <div class="search-form" >
    <div class= "header">
      <%= image_tag 'app_title.png', alt: "relief"%>
      <h3 class="mt-2" >Manage your shared housing today</h3>
    </div>
    <div class="container">
      <div class="row justify-content-center">
          <% unless user_signed_in? %>
          <%= link_to "Sign up", new_user_session_path, class: "btn btn-primary" %>
          <% end %>
      </div>
    </div>
  </div>
</div>
