<div class="container">
  <div class="row">
    <div class="col-6">
      <% @chores.each do |chore| %>
      <%= chore.name %>
      <%= chore.description %>
      <%= chore.frequency %>
      <% end %>
    </div>
  </div>
</div>
