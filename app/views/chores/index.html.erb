<nav aria-label="Page navigation example">
  <ul class="pagination justify-content-center my-0" >
    <li class="page-item disabled">
    </li>
    <li class="page-item"><a class="page-link" href="#">Last week</a></li>
    <li class="page-item"><a class="page-link" href="#">This week</a></li>
    <li class="page-item"><a class="page-link" href="#">Next week</a></li>
    <li class="page-item">
    </li>
  </ul>
</nav>

<div class="d-flex justify-content-center">
  <div class="container-chore">
    <div class="row">
      <div class="col-sm-6">
          <table class="card-chore-table">
            <thead>
              <th></th>
              <% @room_chores.sort_by { |room_chore| room_chore.room.number }.each do |room_chore| %>
              <th><h3 class="d-flex justify-content-center my-3"><%= room_chore.room.number %></h3></th>
              <% end %>
            </thead>
              <tbody>
                <% @room_chores.each do |room_chore| %>
                  <%= render "room_chores/table_row", room_chore: room_chore %>
                <% end %>
              </tbody>
          </table>
      </div>
    </div>
  </div>
</div>
